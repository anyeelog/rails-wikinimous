<div class="container">
  <h1>✨ &nbsp; A R T I C L E &nbsp; L I S T &nbsp; ✨</h1>
</div>

<div class="container">
  <ul class="list_container">
    <% @articles.each do |article| %>
      <li class="list-style"><%=link_to article.title, article_path(article) %></li>
    <% end %>
  </ul>
</div>

<div class="container">
  <%= link_to "Add a new article", new_article_path, class: "index_submit" %>
</div>
